
export enum DayOfWeek {
    Monday=0,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday,
}

export enum LayoutTypes {
    EXTERNAL = 'EXTERNAL',
    INTERNAL = 'INTERNAL'
}

export enum DoseTypes {
    NONE = 'NONE',
    FIRST = 'FIRST',
    SECOND = 'SECOND',
}